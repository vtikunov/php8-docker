account default
host %SMTP_HOST%
port %SMTP_PORT%
domain %SMTP_DOMAIN%
user %SMTP_USER%
password "%SMTP_PASSWORD%"
from "%SMTP_FROM%"
tls on
tls_starttls on
tls_trust_file /etc/ssl/certs/ca-certificates.crt
tls_certcheck on
auth on
