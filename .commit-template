# [<tag>] (При применении этого коммита будет...) <subject> (Максимум 72 символа)
# |<----   Желательно использовать до 50 символов   --->|<------------------->|
# Пример:
# [feat] Внедрение автоматизированной фиксации сообщений

# (Необязательно) Объяснить, почему сделано это изменение
# |<----   Постарайтесь ограничить каждую строку максимум 72 символами   ---->|

# (Необязательно) Приведите ссылки или идентификаторы на соответствующие тикеты, задачи или другие ресурсы
# Пример: Github issue #23

# --- COMMIT END ---
# Метка может быть
#    feat     (новый функционал)
#    fix      (исправление ошибки)
#    refactor (рефакторинг кода)
#    style    (форматирование, отсутствующие отступы и т. д.;  без изменения кода)
#    doc      (изменения в документацию)
#    test     (добавление или рефакторинг тестов; без изменения production кода)
#    version  (увеличение версии/новый релиз; без изменения production кода)
#    license  (изменения, касающиеся лицензирования; без изменения production кода)
#    hack     (временное решение; пожалуйста, избегайте его)
#    defaults (изменения параметров по умолчанию )
#
# --------------------
# Запомните:
#   * Строка темы должна начинаться с заглавной буквы
#   * Не заканчивайте строку темы точкой
#   * Отделяйте строку темы от описания пустой строкой
#   * Используйте описание, чтобы объяснить, что, почему и как
#   * Вы можете образовывать списки с помощью символов "-" или "*" в начале каждой строки списка в описании
# --------------------
